#!/bin/bash

if   [[ "$OSTYPE" == "cygwin" ]]
then
    echo TODO
elif [[ "$OSTYPE" == "msys" ]]
then
    echo TODO
elif [[ "$OSTYPE" == darwin* ]]
then
    open -a /Applications/Atom.app $*
else
    echo "unknown OSTYPE $OSTYPE"
    exit 1
fi

